services:
  - type: web
    name: fake-news-detector
    runtime: python
    pythonVersion: 3.11
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 4 --worker-class sync --timeout 60 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: PORT
        value: 5000
